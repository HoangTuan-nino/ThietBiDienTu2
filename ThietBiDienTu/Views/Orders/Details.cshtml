@model ThietBiDienTu.Models.Order

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<style>

    .card {
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        max-width: 300px;
        margin: auto;
        text-align: center;
    }

    .title {
        color: grey;
        font-size: 18px;
    }
</style>

<h2>Chi tiết hóa đơn</h2>
<hr />
@Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
<div class="card">
    <h2>UserID: @Html.DisplayFor(model => model.UserId)</h2>
    <h3>Ngày: @Html.DisplayFor(model => model.OrderDate)</h3>
    <p>Mô tả: @Html.DisplayFor(model => model.Descriptions)</p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.OrderID }, new { @class = "btn btn-danger" })
</div>
